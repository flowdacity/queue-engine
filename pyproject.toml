[project]
name = "flowdacity-queue"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "argparse>=1.4.0",
    "msgpack>=1.1.2",
    "redis[hiredis]>=7.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.1",
]
